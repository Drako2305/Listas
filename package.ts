{
  "name": "procura-flow",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "dev": "npm run build && npx --yes serve ."
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
